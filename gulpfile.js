var gulp = require('gulp');

var exec = require('child_process').exec;

var path = '~/_Dev/golang/GOPATH/src/github.com/dovneko/bookis/app/app.yaml';

gulp.task('default',function () {
    // exec('dev_appserver.py '+path, function (err, stdout, stderr) {
    //   console.log(stdout);
    //   console.log(stderr);
    // });
  });